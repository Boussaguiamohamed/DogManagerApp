import { HomePage } from "./pages/Homepage"

// The App component is the root component of our application
function App() {
  // Render the HomePage component as the main content of the app
  return <HomePage/>
}

export default App
